{#-
Thiqa SMART style contract - dark variant.

Substitutes templates/api/smart/smart-style_dark.json.twig on the SMART style
route. Locked Q38 / resolution CR-17: this file is reached by rewriting the
template NAME to @oe-module-skyeagle-branding/api/smart/smart-style_dark.json.twig
from a TemplatePageEvent listener. It is NOT a path prepended into the main Twig
namespace, so it can never shadow a core template by resolution order, and the
core file is neither modified nor removed.

Dark is the second of the two locked Saudi variants (Q77). Upstream ships no dark
SMART contract at all, so a SMART app launched from a dark session previously
received light chrome and drew its own light surfaces inside a dark host; these
values are what close that gap.

The twelve keys and their values are the SMART Style contract, copied verbatim
from the generator output (tools/branding/bin/generate-tokens.php over
brand/tokens/thiqa-tokens.json, dark). Regenerate rather than hand-edit: the
generator is the authority for the JSON body below, and CI fails on a body diff
(tools/branding/src/DeployedArtefacts.php compares payloads with this leading
comment stripped, so this prose may differ from the generator banner but the JSON
may not).

Wire format is the core one, unchanged: `logo_primary` is interpolated from the
`logo.primary` variable supplied by SMARTAuthorizationController::smartAppStyles(),
and the controller wraps this rendered document in a JsonResponse, so the payload
third-party SMART apps receive stays double-JSON-encoded exactly as today. The
whitespace-control markers on this comment keep the first emitted byte at `{`.

@package OpenEMR
@subpackage SkyEagleBranding
@author mohammedfouly1 <mselfouly2008@yahoo.com>
@copyright Copyright (c) 2026 mohammedfouly1
@license https://github.com/openemr/openemr/blob/master/LICENSE GNU General Public License 3
-#}
{
    "color_background": "#0B1220",
    "color_error": "#F29088",
    "color_highlight": "#8FC1EE",
    "color_modal_backdrop": "rgba(0, 0, 0, 0.6)",
    "color_success": "#8FD1A6",
    "color_text": "#F5F6F8",
    "dim_border_radius": "6px",
    "dim_font_size": "14px",
    "dim_spacing_size": "20px",
    "font_family_body": "'Inter','IBM Plex Sans Arabic',sans-serif",
    "font_family_heading": "'Inter','IBM Plex Sans Arabic',sans-serif",
    "logo_primary": "{{ logo.primary }}"
}
