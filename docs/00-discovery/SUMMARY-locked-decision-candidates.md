# SUMMARY — Locked Decision Candidates (Phase 0 Synthesis)

- **Fork HEAD:** `631f2b38cf633769c305233f88cdf9c73ca80657` (`01-repo-inventory.md:10`)
- **Effective version:** OpenEMR **8.3.0-dev**, schema `$v_database=541`, ACL `$v_acl=13` (`01-repo-inventory.md:28-29`, `version.php:18-42`)
- **Snapshot date:** 2026-07-04 (HEAD commit date); synthesized 2026-07-20
- **Status:** These are **CANDIDATE** locks derived from the read-only Phase 0–14 audit. Every row below must be either (a) explicitly confirmed by the product owner and moved to `SUMMARY-locked-decisions.md`, or (b) demoted to an entry in `SUMMARY-open-questions.md` if the product owner disagrees.
- **Enforcement contract:** once locked, violation by any AI agent (or human) must be caught at PR review. The "Risk if AI violates it" column exists to make each violation's blast radius concrete.

| # | Domain | Candidate Locked Decision | Evidence (report + file:line) | Confidence | Risk if AI violates it |
|--:|---|---|---|---|---|
| 1 | OpenEMR pinned version | Track upstream at commit `631f2b38` = OpenEMR **8.3.0-dev**, schema **v541**, ACL **v13**. All downstream design assumptions are qualified as-of this HEAD; changes to any of these three numbers force re-audit. | `01-repo-inventory.md:10, 28-34`; `version.php:18-42` | HIGH | AI assumes different version (e.g. 7.0.3 or `master`) → generated code targets FHIR resources / event classes / table columns that don't exist at this HEAD; runtime fatals; wasted implementation cycles. |
| 2 | Upstream tracking policy | Add `upstream` remote → `https://github.com/openemr/openemr.git`; monthly `git fetch upstream --tags`; fork branches always cut from a **tagged upstream release** (never a dev-HEAD); rebase cadence monthly with mandatory zero-core-conflict check. | `01-repo-inventory.md:8, 138-144`; `15-upgrade-and-patch-strategy.md §5, §8` | HIGH | AI performs `git pull` from `origin/master` only → fork silently drifts; upstream security patches missed; next rebase becomes multi-week merge hell; supply-chain CVEs remain unpatched. |
| 3 | PHP version range | Minimum **PHP 8.2.0** (composer platform pin + `Checker.php` runtime gate); CI validates through 8.5 full-test + 8.6 syntax-only. All new code MUST include `declare(strict_types=1)`. | `02-tech-stack.md §1` (`composer.json:14, 243`; `src/Common/Compatibility/Checker.php:19`; `.github/workflows/{syntax,phpstan,isolated-tests,test}.yml`) | HIGH | AI writes PHP 8.1-compatible code (no readonly classes, no enum tricks) → CI green but code is anti-pattern per CLAUDE.md; OR writes 8.6+ syntax → composer resolver rejects at install time; OR omits strict_types → silent int/string coercion bugs enter production. |
| 4 | MariaDB / MySQL floor | Dev-easy container pins **MariaDB 11.8.8**; CI matrix floor is **MariaDB 10.6 / MySQL 5.7**; default charset `utf8mb4` set at server level (not per-table). All new DDL uses `ENGINE=InnoDB` (only engine in baseline). | `02-tech-stack.md §1` table + `docker/development-easy/docker-compose.yml:4`; `.github/workflows/integration-tests.yml:30-51`; `04-database-schema.md §2, §3` | HIGH | AI issues DDL with `ENGINE=MyISAM` or a JSON-column feature requiring MariaDB 10.11+ → breaks on the CI floor and on any customer stuck on 10.6. Uses `utf8` (3-byte) instead of `utf8mb4` → silent Arabic character truncation. |
| 5 | No-core-edit policy (bright-line) | AI MUST NOT modify any file under `src/`, `library/`, `interface/` (except NEW subdirs under `interface/forms/` or NEW dirs under `interface/modules/custom_modules/oe-module-<ours>-*`), `templates/`, `sql/`, `apis/`, `oauth2/`, `gacl/`, `portal/`, `ccdaservice/`, `public/`, `controllers/`, `config/`, `bin/`, `sphere/`, `meta/`, `ccr/`, `_rest_routes.inc.php`, `interface/main/tabs/menu/menus/*.json`, `sites/default/`, or `custom/`. All changes live in `interface/modules/custom_modules/oe-module-<ours>-*/`, `interface/forms/<new-subdir>/`, `sites/<tenant>/` (non-default), or `docs/`. | `03-directory-map.md §1 (rows tagged CORE-DO-NOT-TOUCH & LEGACY), §3` last row; `07-modules-and-extensibility.md §7.1, §7.2`; `15-upgrade-and-patch-strategy.md §3, §5.5, §7` | HIGH | AI patches `src/Billing/Claim.php` to inject NPHIES logic → every monthly rebase yields conflicts on that file; upstream security patches to `Claim.php` are missed or reintroduce our patch backwards; entire fork becomes unmaintainable within 2–3 upstream releases; ONC certification void. |
| 6 | Custom schema namespace rule | All downstream tables prefixed `custom_` or `mod_<module>_` (per the claimrev-connect precedent `mod_claimrev_*`). NEVER add columns to upstream tables in-place — use a `custom_<table>_ext` side table joined by PK. NEVER bump `$v_database` or `$v_acl`; those counters are upstream-owned. Enforced by `SQLUpgradeService` literal-name touching only what it names (`04-database-schema.md §10`). | `04-database-schema.md §7a, §10`; `08-billing-claims-insurance.md §5.3` (mod_claimrev_* precedent); `15-upgrade-and-patch-strategy.md §3, §4` | HIGH | AI adds `ALTER TABLE patient_data ADD COLUMN nphies_national_id` → next upstream upgrade file's `#IfMissingColumn patient_data nphies_national_id` collides or upstream introduces a same-named column with different semantics → data corruption on rebase. Bumps `$v_database=542` → next upstream release also targets 542 → DB thinks it's upgraded but isn't → schema divergence. |
| 7 | Extension mechanism policy | Only sanctioned mechanisms: (a) custom modules under `interface/modules/custom_modules/oe-module-<ours>-*` with `openemr.bootstrap.php` + `ModuleManagerListener.php` extending `AbstractModuleActionListener`; (b) Symfony EventDispatcher subscribers (79 event classes, 22 subject areas); (c) REST/FHIR endpoints via `RestApiCreateEvent`; (d) menu injection via `MenuEvent::MENU_UPDATE` / `PatientMenuEvent`; (e) Twig templates via `TwigContainer::addPath()`; (f) per-tenant overrides via `sites/<tenant>/`; (g) new encounter forms via `interface/forms/<new-subdir>/`. | `03-directory-map.md §2.1-2.7, §3`; `07-modules-and-extensibility.md §1-4, §7.1, §7.2`; `09-frontend-ui.md §7` | HIGH | AI edits `apis/routes/_rest_routes_standard.inc.php` to add a new REST endpoint instead of using `RestApiCreateEvent` → core edit, rebase conflict; OR edits `standard.json` to inject a menu item instead of `MenuEvent::MENU_UPDATE` → same conflict; OR drops a script in `custom/` thinking it's a customization surface → collides with the 20 upstream-tracked files already there. |
| 8 | Auth strategy — federated identity | Keycloak / generic OIDC federation implemented as a Google-Sign-In-shaped provider class in a custom module (`oe-module-<ours>-sso`), mirroring `AuthUtils::verifyGoogleSignIn` (`AuthUtils.php:1443,1476`) which matches by `users.google_signin_email` and calls `setUserSessionVariables()` unchanged. NEVER touch the 12-step `AuthUtils::confirmUserPassword` order-of-operations. Login/logout events don't exist in core — add via upstream PR (class-b commit), NOT a fork patch. | `05-auth-and-acl.md §1 (steps table), §2, §9 (Google Sign-In), §10 (Reusable / Wrapper needed / Conflicts)`; `07-modules-and-extensibility.md §4.2 (gaps)`; `15-upgrade-and-patch-strategy.md §6 (Auth paragraph)` | MEDIUM | AI patches `src/Common/Auth/AuthUtils.php` to add Keycloak branch → conflicts with every upstream auth security patch; users locked out on rebase; timing-attack shim broken; MFA/LDAP flow regressions. |
| 9 | ACL policy — additive only | `AclMain::aclCheckCore` has **307 file call sites** — replacement is infeasible. New roles / permissions ship as additive `INSERT INTO gacl_aro_groups/gacl_aco/gacl_acl` in a module's SQL (`sql/install.sql` via `install_sql` hook), or a per-module `acl_upgrade.php` in the `zend_modules/module/PatientFilter/acl/acl_upgrade.php` pattern. Freeze `$v_acl=13` upstream-owned; do NOT author v14. | `05-auth-and-acl.md §5.1, §5.2, §5.3, §5.4 (v13 versioning), UNKNOWN #4`; `15-upgrade-and-patch-strategy.md §6 (ACL paragraph)` | HIGH | AI writes a new `AclMain` implementation or forks `gacl/` → 307 call sites silently break; every ACL check returns wrong answer; PHI leaks or admin-lockout at scale; audit compliance void. Bumping `$v_acl=14` collides with future upstream v14. |
| 10 | API strategy | REST + FHIR R4 (**version 4.0.1** hard-coded at `FhirMetaDataRestController.php:59-61`) + US Core **3.1.0** (as-implemented, pinned by route-map filename `_rest_routes_fhir_r4_us_core_3_1_0.inc.php`). All new endpoints MUST be added via `RestApiCreateEvent` subscriber calling `addToRouteMap()` / `addToFHIRRouteMap()` / `addToPortalRouteMap()`. No numeric URL versioning (no `/v1/`, `/v2/`); breaking-change strategy is UNKNOWN and deferred to product owner. | `06-api-surface.md §1-5, §7, §9`; `07-modules-and-extensibility.md §4.1 (RestApiCreateEvent)`; `apis/routes/_rest_routes_fhir_r4_us_core_3_1_0.inc.php` | HIGH | AI hand-edits `apis/routes/*.inc.php` → core-edit rebase conflict; OR advertises US Core 6.x compliance without upgrading services → false certification claim. |
| 11 | FHIR write-surface caveat | Today ONLY `Patient`, `Practitioner`, `Organization` accept POST/PUT (routing PUT internally to `->patch()`). All other 29+ FHIR resources are **read-only**; NONE support HTTP `PATCH` or `DELETE`. NPHIES-critical resources (`Claim`, `ClaimResponse`, `Coverage` write, `CoverageEligibilityRequest`, `CoverageEligibilityResponse`, `ExplanationOfBenefit`, `PaymentNotice`) are **not routed at all** — greenfield builds via new `FhirService` classes + new `FhirRestController` classes + `RestApiCreateEvent` subscribers. | `06-api-surface.md §4, §12`; `08-billing-claims-insurance.md §4 Option A, §7 UNKNOWN #1` | HIGH | AI assumes `POST /fhir/Claim` "just works" → design docs promise NPHIES claim submission via unmodified upstream endpoint that doesn't exist; delivery slip; discovery re-run required mid-sprint. |
| 12 | Multi-tenant model | **DB-per-tenant using native OpenEMR multisite** (`sites/<tenant>/` + per-site `sqlconf.php` → own MySQL DB). Subdomain-to-site resolution requires small patch to `interface/globals.php:284-297` lifting `HTTP_HOST` fallback out of the `$ignoreAuth` gate — route as **upstream PR** not fork patch. NEVER attempt shared-DB with `tenant_id` column — lower bound is **1,875 `sqlStatement(` call sites across 453 files** plus ~40 `OE_SITE_DIR` file-path sites, and every future upstream commit adds more. | `10-multisite-multitenant.md §1-6, §10 Model A vs B, §11`; `15-upgrade-and-patch-strategy.md §6 (Multi-tenant paragraph)` | HIGH | AI proposes/starts shared-DB refactor → 1,875+ query sites must be rewritten and every future upstream patch re-scoped; project unshippable. Silently adds `tenant_id` column to `patient_data` etc. → breaks upgrade path per rule #6. |
| 13 | Session isolation caveat | `CORE_SESSION_ID = "OpenEMR"` is a global constant (`SessionUtil.php:81`) not suffixed by site — a browser can only be logged into ONE tenant at a time; cross-tenant navigation force-clears the session (`interface/globals.php:310-323`). Either **accept this limitation** and enforce subdomain-per-tenant with distinct cookie scopes, OR plan an explicit per-site cookie-name patch (upstream PR). | `05-auth-and-acl.md §6`; `10-multisite-multitenant.md §7, §10 Model A "Cookie-per-site" gap`; `14-security-compliance.md §12 "Session isolation across tenants" row` | HIGH | AI designs multi-tenant admin console that assumes one browser can hold multiple simultaneous tenant sessions → UX breaks the first time an admin opens two tenants → session collision wipes both; support burden. |
| 14 | Docker baseline | Layer downstream image `FROM openemr/openemr:flex-edge` (or a pinned release tag). NEVER fork `docker/flex/Dockerfile` (354 lines, continuously churned upstream). Add modules via Composer (`openemr/oe-module-installer-plugin`) + first-boot auto-provisioning via `OPENEMR_SETTING_<gl_name>=<value>` env-var pattern (`devtoolsLibrary.source:134-164`). | `11-devops-docker-ci.md §1, §2.1, §2.3, §2.4, §7`; `15-upgrade-and-patch-strategy.md §6 (Docker paragraph)` | HIGH | AI forks the Dockerfile to add PHP extensions or Alpine packages → loses every upstream security update to base image; PHP 8.6 upgrade must be re-done manually; image size drifts; kcov / xdebug / entrypoint changes silently skipped. |
| 15 | Frontend strategy — bounded | Three-track approach: (a) **restyle** existing Bootstrap 4.6 themes via new SCSS entrypoints or per-tenant `css_header` swap; (b) **Twig template additions** shipped by custom modules via `TwigContainer::addPath()` (autoescape is OFF globally — module code MUST escape explicitly with `text/attr/js_escape/xlt`); (c) OPTIONAL new SPA for the patient portal only (portal is already a separate app with separate credentials in `patient_access_onsite`). DO NOT attempt to replace the main-UI shell (`main.php` is 577 lines of Knockout MVVM wiring 1057+ legacy tab-iframe pages). | `09-frontend-ui.md §1-4, §10 (effort classes a/b/c/d/e)`; `02-tech-stack.md §2.1 (autoescape OFF)`; `14-security-compliance.md §2.3` | MEDIUM | AI proposes React-shell replacement → quarters of effort, reimplements 1057 legacy screens, forfeits event/menu extensibility; project derails. Ships Twig without escaping → XSS in every custom-module page. |
| 16 | i18n / Arabic constraints | Extend Arabic translations via `lang_custom` table (`sql/database.sql:3578`). DO NOT edit `contrib/util/language_translations/currentLanguage_utf8.sql` (upstream master, regenerated by Perl toolchain). `bootstrap-rtl` napa dep pins an unmaintained single-commit third-party fork (`package.json:112-113`) — **must be re-vendored to our own maintained fork**. Arabic-typography PDF fonts (Amiri or Noto Naskh Arabic) MUST be shipped — repo currently only carries mpdf's transitive DejaVu Sans, insufficient for polished output. **Hijri calendar has ZERO support anywhere** — completely greenfield. | `13-i18n-localization.md §2, §3, §4 (bootstrap-rtl), §5 (Hijri = none), §10 rows, UNKNOWN #9`; `09-frontend-ui.md §6 (PDF Arabic fonts)` | HIGH | AI edits `currentLanguage_utf8.sql` → next upstream sync rewrites the whole file, translations lost. Assumes `bootstrap-rtl` will keep working → build breaks day the GitHub archive URL goes away. Writes Hijri date handling as if `moment-hijri` or `IntlCalendar` were already wired → runtime error; Hijri needs new library+wrapper. |
| 17 | Currency / VAT / ZATCA | **No `currency` column exists on `billing`, `insurance_companies`, `ar_session`, `ar_activity`, `fees`** — currency is a single site-wide `gbl_currency_symbol` string (default `'$'`); FHIR Coverage hard-codes `'USD'` at `FhirCoverageService.php:294`. **No ZATCA/e-invoice/QR/Fatoora hooks anywhere** (grep returns zero across `*.php,*.js,*.sql,*.json,*.yml`). SAR display, VAT tax-amount columns, ZATCA XML signing + invoice hash + QR + Fatoora integration are **all greenfield module builds**. | `13-i18n-localization.md §6, §8, §10 rows`; `08-billing-claims-insurance.md §6 (Currency-per-payer), §8.3 (Replace / new-build)` | HIGH | AI assumes FHIR Coverage payload will emit SAR because "currency is configurable" → sends USD to Saudi payer; NPHIES claim rejected. Skips ZATCA scope thinking it's a config toggle → hard delivery slip when auditor rejects invoices without XML signature + QR. |
| 18 | Billing-layer reuse boundaries for NPHIES | **REUSE:** fee sheet (`interface/forms/fee_sheet/*`), `billing`/`claims`/`ar_session`/`ar_activity` state machine, `codes`+`code_types` overlay (add Saudi CCHI code-types), primary/secondary/tertiary coverage chain, `background_services` scheduler, patient-responsibility `payer_id=0` sentinel. **REPLACE:** X12 5010 837P/837I codecs (`X125010837P.php`, `X125010837I.php`), X12 270/271 (`EDI270.php`), X12 835 (`ParseERA.php`), CMS-1500 / UB-04 generators, 10-digit NPI validators, `x12_partners` ISA/GS envelope config. **NEW BUILD:** FHIR `Claim`/`ClaimResponse`/`CoverageEligibility*`/`PaymentNotice` services + `RestApiCreateEvent` route registrations + `mod_nphies_*` shadow tables (mirror claimrev-connect's `mod_claimrev_*` pattern) + PaymentNotice consumer writing to `ar_session`/`ar_activity`. | `08-billing-claims-insurance.md §3.1, §3.2, §5.5, §8.1, §8.2, §8.3` | HIGH | AI patches `X125010837P.php` to emit NPHIES XML → mutates core codec, breaks US-market rebase, gets overwritten every upstream release, and NPHIES isn't X12 anyway. Skips the shadow-table pattern → NPHIES claim state written back into core `claims` table with X12-specific fields left NULL, breaking existing reports. |
| 19 | Testing baseline | **REUSE:** PHPUnit two-tier (`phpunit.xml` DB-backed + `phpunit-isolated.xml` pure-PHP); Panther/Selenium E2E harness (`tests/Tests/E2e/Base/BaseTrait.php` + `docker-compose.yml:126` selenium service); `ApiTestClient` (OAuth2-wired REST tests); custom PHPStan rules under `tests/PHPStan/Rules/` (18 rules incl. `ForbiddenGlobalsAccessRule`, `ForbiddenRequestGlobalsRule`, `ForbidDirectSessionWriteRule`, `ForbiddenClassesRule` for Laminas-DB); fixture-manager prefix pattern. **NEW BUILD:** `TenantIsolationTestCase` (no prior art); NPHIES HTTP mock (Guzzle MockHandler); Arabic/RTL i18n test suite (zero existing coverage); DB reset/truncation between tests (prefix cleanup is fragile). | `12-testing-infrastructure.md §1-12`; `11-devops-docker-ci.md §4.1` (CI wired to reusable `test.yml`) | HIGH | AI writes tests bypassing PHPStan-forbidden globals (`$_SESSION`, `$_GET`, `$GLOBALS`) → CI fails; OR writes tests that assume TransactionalTestCase exists → tests silently pollute the shared DB → flaky suite; OR writes NPHIES integration tests hitting real Saudi sandbox → cost + rate limits. |
| 20 | Security — encryption | REUSE `OpenEMR\Common\Crypto\CryptoGen` (AES-256-CBC + encrypt-then-MAC with HMAC-SHA384, versioned envelope via `KeyVersion`, dual-key set: DB `keys` table + drive-encrypted files in `sites/.../methods/`). **BUT: `CryptoGen` has no KMS / TPM / env-var key source and is not AEAD.** Per-tenant KMS integration is a **wrapper we MUST build** (implement `CryptoInterface` to envelope-encrypt DB `keys` rows with per-tenant cloud-KMS CMK). Document as "wrapper needed"; do not fork `CryptoGen` itself. | `14-security-compliance.md §5, §12 "Per-tenant crypto keys" & "Per-tenant KMS" rows` | HIGH | AI forks `CryptoGen.php` to add KMS branch → core edit; every upstream security patch to CryptoGen (there have been several — see `KeyVersion` history) must be manually re-applied; key-rotation logic diverges silently → decrypt-failures with cross-version ciphertexts. |
| 21 | Security — audit logging | REUSE `EventAuditLogger` singleton (writes to `log`/`api_log`/`extended_log`/`audit_master`) with its **dedicated DB connection** (separate from main app conn, essential for `LogTablesSink`/`BreakglassChecker`) and optional ATNA sink for IHE syslog. **BUT: no retention/rotation exists** — `log` and `api_log` grow unbounded. Wrap with: (a) per-tenant retention policy + scheduled pruner (`background_services` row); (b) Saudi PDPL export API for tenant-admin data-subject requests; (c) per-tenant syslog forwarder. All wrappers ship as `oe-module-<ours>-audit-extensions`, never as edits to `src/Common/Logging/*`. | `14-security-compliance.md §6, §12 "Per-tenant audit-log export" & "Log retention" rows` | HIGH | AI silently truncates `log`/`api_log` to save space → destroys HIPAA/PDPL audit trail; OR patches `EventAuditLogger` to add tenant column → core edit + schema break on rebase. |
| 22 | Security — hardcoded token incident (day-1 remediation) | `docker/development-easy/docker-compose.yml:75-77` contains three credential-shaped values: one 40-char lowercase-hex legacy token (line 75) plus TWO obfuscated modern GitHub PATs decoded as `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` (base64, line 76) and `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` (decimal-array, line 77). ALL THREE must be **treated as burned regardless of live/revoked status**: revoke at GitHub, purge from git history (`git filter-repo` / BFG), replace with buildkit-secret or `.env` injection at compose-up time, add gitleaks/trufflehog pre-commit + CI scan for `ghp_*` and obfuscated variants. | `14-security-compliance.md §9, UNKNOWN §14 last two rows` | HIGH | AI leaves them in place "because they're just dev tokens" → any future public fork/mirror publishes them; GitHub rate-limits or bans the entire org; credential-scanner alerts to compliance auditor; if either PAT ever had write scope to a private repo, that repo is compromised. |
| 23 | CI/CD | REUSE `.github/workflows/test.yml` reusable workflow + `test-all.yml` matrix + `isolated-tests.yml` + `phpstan*.yml` + `rector.yml` + `styling.yml` + `pre-commit.yml` + `js-test.yml` + `semgrep.yml` + `conventional-commits.yml`. **ADD:** a fork-specific `test-<ours>.yml` workflow that runs our SaaS custom-modules test suite (tenant isolation, NPHIES mocks, Arabic UI). **DROP** from our fork: all `docker-build-*` / `docker-push-dockerhub-readme.yml` / `docker-release-orchestrator.yml` / `branch-cut-automation.yml` / `patch-prep-automation.yml` / `notify-release-targets-changed.yml` / `refresh-reserved-word-supplement.yml` / `inferno-test.yml` (unless ONC cert in scope). Prune the `ci/` matrix to configs we actually ship (drop `mysql_57`, `mariadb_10.6` if unused). | `11-devops-docker-ci.md §4.1, §5, §7 (table row "GitHub Actions workflows"), UNKNOWN #2 & #3` | MEDIUM | AI enables `docker-push-dockerhub-readme.yml` on the fork → attempts to push to `openemr/openemr` Docker Hub org and fails or (worse) succeeds with credentials that shouldn't work; OR leaves upstream `branch-cut-automation.yml` enabled → automated PRs against upstream release branches that don't apply to us. |
| 24 | Doctrine Migrations | **Scaffolded but NOT authoritative per `db/README.md:10-12`** — "The Doctrine Migrations system is NOT fully integrated into OpenEMR yet. Don't make database changes using this until #10708 is completed." Only one placeholder migration exists (`db/Migrations/Version00000000000000.php`). USE legacy `sql/*_upgrade.sql`-shaped files INSIDE our custom modules (`sql/install.sql` + `sql/upgrade/<from>-to-<to>.sql` applied via `install_sql` / `upgrade_sql` hooks in `AbstractModuleActionListener`). Monitor upstream issue #10708 for when Doctrine becomes authoritative — trigger a policy revisit at that point. | `04-database-schema.md §7c`; `15-upgrade-and-patch-strategy.md §1 (row 4), §5 UNKNOWN #6` | HIGH | AI adds Doctrine migration classes under `db/Migrations/` for our schema changes → `./cli migrate` runs them but the legacy `sql_upgrade.php` path (still authoritative) doesn't know they ran → double-apply or state divergence; other tenants stuck on legacy path get out of sync. |
